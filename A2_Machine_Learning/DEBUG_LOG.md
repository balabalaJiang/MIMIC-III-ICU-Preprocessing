# 實驗除錯紀錄 (DEBUG LOG)
**學號：** `1258001`  |  **姓名：** `江永閎`

| 紀錄編號 | 發生環節 | 錯誤訊息 / 異常現象 | 原因分析 | 解決方案 | 狀態 |
|:---:|---|---|---|---|:---:|
| **01** | Step 2: 抽樣子集函式 | (潛在問題) 抽樣合併後，若直接進行 `train_test_split`，可能因為資料順序是「先排全部少數類，再排全部多數類」而導致切割出錯。 | 合併 index 後，資料具有強烈的排列規律性。 | 在函式最後一行加入 `sklearn.utils.shuffle(X_sub, y_sub, random_state)`，強制打散樣本順序，確保後續實驗不受順序干擾。 | ✅ 已解決 |
| **02** | Step 2: 方案 A 資料載入 | 出現 `ValueError: 原始資料不足以抽取 500 筆`。檢查發現 `y=1` 比例為 0.0000，且 `X shape` 高達 17065 維。 | HW1 的特徵工程不慎將目標變數 `ExpiredHospital` 進行了標準化（導致平均值變為 0），並將流水號 `hadm_id` 等高基數欄位做了 One-Hot，導致維度災難與標籤毀損。 | 依照作業規範建議，果斷捨棄 HW1 結果，切換至 **方案 B**。在 `load_mimic3c()` 內直接讀取原始 `mimic3c.csv`，重做基礎補值與排除洩漏/ID欄位，成功將特徵控制在合理維度，且目標標籤恢復正常。 | ✅ 已解決 |
| **03** | Step 3: 多模型迴圈訓練 | (潛在問題) 如果在迴圈中直接使用 `model.fit()`，神經網路 (MLP) 或樹模型可能會因為連續訓練而殘留上一輪 split 的權重，導致 F1 score 無法真實反映當次抽樣的表現。 | Scikit-learn 的 estimator 是一個 stateful (具狀態) 的物件，若不重新實例化，會發生狀態記憶現象。 | 在 `fit_and_score()` 函數中，強制使用 `sklearn.base.clone(model_template)`。每次訓練都生出一個全新的、未曾訓練過的乾淨模型，確保這 840 次實驗都是完全獨立的。 | ✅ 已解決 |
| **04** | Step 4: Paired t-test | (潛在問題) 使用 `scipy.stats.ttest_rel` 計算成對 t 檢定時，如果 Stratified 和 Random 陣列裡面的 seed 順序沒有嚴格對齊，會算出完全錯誤的 p-value。 | DataFrame 經過各種篩選後，無法保證 row 的順序仍按照 seed 0~19 嚴格排列。 | 在提取數值前，強制加上 `.sort_values('seed')`，確保兩組陣列在空間上的 index 是完全對應同一次資料切分的，保證了配對檢定的數學與統計正確性。 | ✅ 已解決 |